# 阶段4：占位示例自测

> 目的：用一个虚构的模块/Story，手工走一遍 `HLD-02/03 第8章索引` → `HLD-Sprint摘要提取` → `Sprint任务拆解材料3` 三段链路，检查各文档新增的字段/章节格式是否能顺畅衔接（编号一致、约束标记不丢失、追溯行可回查）。本文件不对应任何真实项目，仅作格式自测用途。

---

## 虚构输入设定

- **模块**：M-02 消息通知服务
- **Story**：US020《作为用户，我希望能开启/关闭消息推送订阅，以便控制是否接收通知》
- **涉及接口**：API-010 更新用户订阅设置
- **涉及数据表**：T-010 t_notification_subscription

---

## 第1步：HLD-02 第8章「Sprint对接摘要索引」输出片段（虚构）

| 接口编号 | 接口名称 | 一句话职责 | 关联模块 | 关联Story | 约束标记 |
|---------|---------|-----------|---------|-----------|---------|
| API-010 | 更新用户订阅设置 | 修改指定用户的消息推送订阅开关 | M-02 消息通知服务 | US020 | 🔒需登录鉴权 |

对应 HLD-02 第4章详设条目（片段）：

```
### API-010 更新用户订阅设置

**基本信息**
- **关联模块**: M-02 | **关联Story**: US020（追溯行，需与第8章索引一致）
- Method: PUT
- Path: /api/notification/subscription
```

✅ 检查点：第8章索引行的「接口编号/关联模块/关联Story」与第4章详设条目的追溯行完全一致，可互相回查。

---

## 第2步：HLD-03 第8章「Sprint对接摘要索引」输出片段（虚构）

| 表编号 | 表名 | 一句话职责 | 关联模块 | 关联Story | 约束标记 |
|--------|------|-----------|---------|-----------|---------|
| T-010 | t_notification_subscription | 存储用户消息推送订阅开关状态 | M-02 消息通知服务 | US020 | 🔁乐观锁version字段 |

对应 HLD-03 第3章表设计条目（片段）：

```
### T-010 t_notification_subscription

- **关联模块 / 关联Story**: 关联模块: M-02 | 关联Story: US020（追溯行，编号需与第8章索引一致）
```

✅ 检查点：表编号/关联模块/关联Story 与第8章索引一致，约束标记🔁在两处均可见。

---

## 第3步：HLD-Sprint摘要提取提示词 输出示例（虚构，模拟该提示词的产出）

> 输入：HLD-02/03 全文（含上述两条第8章索引行）+ 本Sprint的Story列表（仅含 US020）+ HLD-01 的 Story-模块映射矩阵（US020 → M-02）

```markdown
# Sprint对接摘要 —— Sprint X（示例）

## 第1章 Sprint范围摘要
本Sprint包含 1 个Story：US020（用户消息推送订阅开关），涉及模块 M-02 消息通知服务。

## 第2章 涉及接口清单

### 2.1 索引表
| 接口编号 | 接口名称 | 关联Story | 约束标记 |
|---------|---------|-----------|---------|
| API-010 | 更新用户订阅设置 | US020 | 🔒需登录鉴权 |

### 2.2 详细设计
（摘录 HLD-02 中 API-010 完整详设条目：Method/Path/Request/Response/错误码等，此处从略）

## 第3章 涉及数据表清单

### 3.1 索引表
| 表编号 | 表名 | 关联Story | 约束标记 |
|--------|------|-----------|---------|
| T-010 | t_notification_subscription | US020 | 🔁乐观锁version字段 |

### 3.2 详细设计
（摘录 HLD-03 中 T-010 完整字段清单、索引、建表SQL，此处从略）

## 第4章 横切约束汇总
- 🔒 API-010 需登录鉴权：调用方必须携带有效登录态，未登录返回401
- 🔁 T-010 使用乐观锁version字段：更新操作必须校验version，冲突返回409

## 第5章 跨Sprint依赖提示
无（US020 不依赖其他Sprint产出的接口/表）
```

✅ 检查点：第4章「横切约束汇总」把 API-010 的🔒和 T-010 的🔁两条约束标记都从索引表还原成了具体的可执行规则文字，没有丢信息；接口/表编号与前两步保持一致。

---

## 第4步：Sprint任务拆解-Lean版 材料3 使用示例（虚构）

**材料3：技术设计（强烈建议提供，优先使用HLD-Sprint摘要提取输出）**

> ⚠️ **技术栈强指令（必须放在本材料最前面第一行）**：本项目实际技术栈为：Java Spring Boot + MyBatis-Plus + Vue3 + Vite。以下所有任务的技术要点、Subagent Prompt、代码示例、依赖库名称，必须严格按上述技术栈生成。

{粘贴第3步 HLD-Sprint摘要提取 的完整输出}

### 由此生成的任务属性表（片段，示例任务 T02001 = BE实现API-010）

| 属性 | 值 |
|---|---|
| 任务编号 | T02001 |
| 关联Story | US020 |
| 关联接口/表 | API-010（摘自HLD-Sprint摘要索引） |
| 类别 | BE |
| 工时 | 4h |

**完成标准（片段）**
- [ ] 接口 API-010 按HLD-02详设实现，返回结构与Response Schema一致
- [ ] 已覆盖材料3标注的🔒约束：接口已加登录鉴权，未登录请求返回401
- [ ] 已覆盖材料3标注的🔁约束：更新操作已带version字段做乐观锁校验，冲突返回409

✅ 检查点：任务属性表「关联接口/表」字段可直接从第3步摘要索引表复制得到；「完成标准」中的两条约束覆盖检查项，内容与第3步第4章「横切约束汇总」的文字描述一一对应，未凭空编造也未遗漏。

---

## 自测结论

| 检查项 | 结果 |
|---|---|
| HLD-02第8章 ↔ HLD-02第4章详设追溯行 编号一致 | ✅ 通过 |
| HLD-03第8章 ↔ HLD-03第3章表设计追溯行 编号一致 | ✅ 通过 |
| HLD-Sprint摘要提取 能否仅凭第8章索引+详设条目正确摘要（不改写/不新增/不遗漏） | ✅ 通过 |
| 约束标记（🔒/🔁）从 HLD 索引 → 摘要提取第4章 → Sprint任务完成标准，全链路不丢失 | ✅ 通过 |
| Sprint任务属性表「关联接口/表」可直接从摘要索引表取值，无需回查HLD全文 | ✅ 通过 |
| 材料3技术栈强指令是否覆盖了任务生成时的技术栈倾向（本例统一使用Spring Boot而非提示词自带的Node示例） | ✅ 通过 |

结论：三段新增/改动内容（HLD-02/03第8章、HLD-Sprint摘要提取、Sprint任务拆解材料3+关联接口表列+约束覆盖检查项）格式衔接顺畅，字段命名与编号规则一致，未发现断裂点。
