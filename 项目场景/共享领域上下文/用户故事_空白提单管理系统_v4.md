# 用户故事 · 空白提单管理系统

> **版本**：v2.1（基于业务澄清回复 + UI规范文档对齐，遵循 Deerpowers user-story-generation 技能规范）  
> **日期**：2026-03-30  
> **产品**：中外运海运空白提单管理  
> **编写人**：需求工程师  
> **所属产品类型**：B端管理系统（货代物流内部系统）  
> **设计资产参考**：企业组件库 v3.2.1  
> **响应式要求**：仅 PC 端  
> **核心页面类型**：列表页、表单页、详情页、弹窗  
> 
> ### 全局 UI 规范（来源于「ui 规范.md」）
> 
> **页面路径**：`文件中心 > 首页 > 个人任务 > 空白提单管理`，当前页高亮  
> 
> **色彩规范**：
> | 类型 | 色值 | 应用场景 |
> | ---- | ---- | ---- |
> | 主色 | `#1677FF` | 导航高亮、主按钮（新增/查询）、文字强调 |
> | 警示色 | `#F53F3F` | 作废按钮、错误状态、删除操作 |
> | 辅助色 | `#8C8C8C` | 普通辅助按钮、次要文字、未激活状态 |
> | 表头背景 | `#F5F7FA` | 列表表头、弹窗标题栏背景 |
> | 页面背景 | `#F9FAFC` | 页面整体底色 |
> | 内容区背景 | `#FFFFFF` | 输入框、列表、弹窗内容背景 |
> | 边框色 | `#E5E6EB` | 输入框、按钮、列表、弹窗边框 |
> | 悬浮高亮 | `#F0F7FF` | 按钮/列表行 hover 背景 |
> 
> **按钮规范**：
> - 主按钮：背景 `#1677FF`，文字 `#FFFFFF`，边框 `#1677FF`，圆角 4px，Hover `#0F66E0`
> - 辅助按钮：背景 `#FFFFFF`，文字 `#8C8C8C`，边框 `#E5E6EB`，Hover `#F5F7FA`
> - 警示按钮：背景 `#F53F3F`，文字 `#FFFFFF`，边框 `#F53F3F`，Hover `#D93636`
> - 禁用状态：透明度 0.6，禁止点击
> 
> **字体规范**：
> - 导航/标题：14px，常规字重，`#333333`
> - 正文/列表：14px，常规字重，`#333333`
> - 次要文字：12px，`#8C8C8C`
> - 按钮文字：14px，常规字重
> 
> **布局尺寸**：
> - 导航栏高度：48px
> - 筛选栏高度：40px
> - 输入框/下拉框/按钮高度：32px
> - 列表行高：40px
> - 表头高度：40px
> - 控件间距：水平 12px，垂直 16px
> - 页面边距：内容区左右内边距 16px
> 
> **交互规范**：
> - 重置按钮清空所有筛选条件，恢复初始默认值
> - 查看→弹出「提单编号查看」明细弹窗
> - 回收/作废仅对选中行生效；操作完成后自动刷新列表
> - 操作反馈：页面顶部 Toast 轻提示
> - 列表支持单选/批量选择，表头可排序

---

## 用户故事总览

| 故事ID | 故事标题 | 用户角色 | 功能分组 | 粒度 | INVEST+D | 状态 |
|--------|---------|---------|---------|------|----------|------|
| US001 | 空白提单号码段入库 | 制单岗 | 入库管理 | M | ✅ | 🟢 通过 |
| US002 | 空白提单号码段编辑 | 制单岗 | 入库管理 | S | ✅ | 🟢 通过 |
| US003 | 号码段自动归档 | 系统 | 入库管理 | S | ✅ | 🟢 通过 |
| US004 | 入库列表筛选与分页 | 制单岗 | 入库管理 | S | ✅ | 🟢 通过 |
| US005 | 通过提单号查询序列号详情 | 制单岗 | 查询管理 | M | ✅ | 🟢 通过 |
| US006 | 序列号详情页查看 | 制单岗 | 查询管理 | S | ✅ | 🟢 通过 |
| US007 | 序列号回收（单条+批量） | 制单岗 | 回收作废 | M | ✅ | 🟢 通过 |
| US008 | 序列号作废 | 制单岗 | 回收作废 | M | ✅ | 🟢 通过 |
| US009 | 导出作废清单 | 制单岗 | 数据导出 | S | ✅ | 🟢 通过 |
| US010 | 操作日志审计查询 | 主管/审核岗 | 审计管理 | S | ✅ | 🟢 通过 |

**统计信息**
- 故事总数：10 个
- 角色分布：制单岗(8个)、主管/审核岗(1个)、系统(1个)
- 粒度分布：S(6个)、M(4个)
- 设计维度覆盖：10/10 全部通过

---

## 功能分组1：入库管理

---

### US001: 空白提单号码段入库

**用户故事**
> 作为 制单岗（仅可管理本港口/本船司的空白提单入库操作，不可跨港口操作），
> 我希望 按船司录入空白提单号码段（起始号+结束号），系统自动生成该号段内所有序列号并统一管理，
> 以便 建立空白提单的数字化台账，减少100%的手工Excel登记工作（从每批5分钟缩短至1分钟内完成），且入库响应时间≤2秒。

**功能要点**
- 核心路径（必做）：选择船司（下拉必选）→ 输入起始号（8位数字）→ 输入结束号（8位数字）→ 系统自动计算总数量 → 填写备注（选填）→ 选择入库日期（默认今日）→ 点击「保存」→ 系统校验号码段 → 生成序列号列表
- 异常路径（必做）：① 起始号格式非8位纯数字时，实时提示"请输入8位纯数字"；② 号码段与已有号段部分交叉时，提示"号码段与已有号段（xxx~xxx）存在交叉，请修改"；③ 结束号 ≤ 起始号时，提示"结束号必须大于起始号"
- 边界条件（可选）：单个号码段最大支持 99999 张（起始号~结束号跨度≤99999）

**业务规则**
- 强制规则（不可协商）：
  - 序列号格式：`船司代码-8位连续数字`（如 COSCO-00012345），船司代码 3~4 位
  - 入库时**必选船司**，系统自动拼接前缀，入库人无需手动输入
  - 起始号与结束号必须连续，不允许间断
  - 同一船司下，新录入号段与已有号段不允许部分交叉或完全重叠；紧邻不重叠允许
  - 已作废号码段自动释放区间，允许重新录入
  - 保存按钮点击后禁用 3 秒防止重复提交
- 柔性规则（可协商）：
  - 备注默认为空，支持自由输入
  - 入库日期默认填充当日日期，可手动修改

**UI/交互设计约束**

**页面上下文**
- 所属页面：空白提单管理-入库弹窗（从列表页触发）
- 页面层级：`文件中心 > 首页 > 个人任务 > 空白提单管理` → 入库弹窗（二级弹窗）
- 入口位置：空白提单管理列表页顶部操作区「新增」按钮（主按钮，背景 #1677FF，文字 #FFFFFF，高度 32px，圆角 4px）
- 退出/返回逻辑：保存成功后关闭入库弹窗，自动刷新列表页；点击「取消」关闭弹窗不保存

**核心交互行为**
- 操作控件：
  - 船司选择：下拉选择器（单选，高度 32px，宽度 200px），选项来源系统船司主数据，支持模糊搜索
  - 起始号/结束号：单行文本输入框（高度 32px，宽度 200px，仅允许输入数字，最大长度 8 位）
  - 数量展示：文本标签（字号 14px，颜色 #1677FF，加粗），格式"共 {n} 张"，紧跟结束号输入框右侧（margin-left: 12px），跟随结束号输入实时更新
  - 备注输入：多行文本输入框（高度 80px，最大长度 200 字）
  - 入库日期：日期选择器（下拉式，高度 32px，默认今日，不可选未来日期）
  - 保存按钮：主按钮（背景 #1677FF，文字 #FFFFFF，边框 #1677FF，宽度 100%，高度 32px，圆角 4px，Hover #0F66E0）
  - 取消按钮：辅助按钮（背景 #FFFFFF，文字 #8C8C8C，边框 #E5E6EB，宽度 100%，高度 32px，圆角 4px，Hover #F5F7FA）
- 交互流程：
  1. 弹窗打开后，船司选择器默认获取焦点（下拉展开提示"请选择船司"）；
  2. 选择船司后，焦点自动跳转至起始号输入框；
  3. 输入起始号时，仅允许数字输入（非数字字符自动过滤）；
  4. 输入完起始号后 Tab 至结束号，输入结束号后系统实时计算并显示数量；
  5. 点击「保存」→ 触发号码段交叉校验 → 校验通过则显示加载状态 → 保存成功关闭弹窗；
  6. 点击「取消」→ 无确认弹窗，直接关闭。
- 焦点与操作逻辑：
  - 起始号/结束号未填写时，点击「保存」→ 输入框边框标红（#F53F3F）+ 底部显示"请输入起始号/结束号"；
  - 号码段交叉校验失败时，结束号输入框边框标红 + 底部显示交叉提示（含冲突号段信息）；
  - 支持 Tab 键切换焦点（船司→起始号→结束号→入库日期→备注→保存→取消）。

**视觉/布局约束**
- 核心元素布局：
  - 弹窗整体：居中弹窗，宽度 560px，高度自适应（最大高度 600px，超出显示滚动条），距顶部 15vh；
  - 弹窗遮罩：全屏遮罩，背景 rgba(0,0,0,0.45)，点击遮罩不关闭弹窗；
  - 弹窗头部：标题"空白提单入库"（字号 14px，常规字重，颜色 #333333，背景 #F5F7FA），右侧关闭按钮（×图标，24px，颜色 #86909C）；
  - 表单区域：单列布局，每个表单项垂直间距 16px，控件水平间距 12px，左右内边距 16px；
  - 标签样式：表单项标签居左，宽度 100px，字号 14px，颜色 #333333，必填项标签后标" *"（颜色 #F53F3F）。
- 控件样式规范：
  - 输入框：高度 32px，默认边框 #E5E6EB，聚焦边框 #1677FF（伴随 1px 阴影 0 0 0 2px rgba(22,119,255,0.1)），错误边框 #F53F3F；
  - 下拉框：高度 32px，边框 #E5E6EB，聚焦边框 #1677FF；
  - 数量标签：紧跟结束号输入框右侧（margin-left: 12px），字号 14px，颜色 #1677FF，加粗；
  - 提示文字：错误提示字号 12px，颜色 #F53F3F，位于输入框底部，距输入框 4px。
- 页面背景色：#F9FAFC；内容区背景：#FFFFFF
- 响应式要求：仅 PC 端（屏幕宽度≥1200px），不兼容平板/移动端。

**交互反馈规则**
- 加载状态：
  - 保存按钮点击后→ 按钮文本变为「保存中...」→ 右侧显示圆形加载动画（直径 14px，白色，旋转周期 1s）→ 按钮禁用（透明度 0.6）→ 持续禁用 3 秒防重复；
  - 号码段校验中→ 保存按钮显示加载状态。
- 成功反馈：
  - 保存成功→ 关闭弹窗→ 页面顶部 Toast 轻提示（3 秒后自动消失）；
  - 提示内容：成功图标 → 文本"入库成功！已生成 {n} 条序列号"（字号 14px）。
- 失败反馈：
  - 保存失败→ 弹窗顶部显示红色提示条（高度 32px，背景色 #FFF1F0）；
  - 提示内容：错误图标 → 文本"保存失败：{具体原因}"（字号 14px，颜色 #F53F3F）→ 右侧关闭按钮（×，16px）。

**设计资产关联**
- 组件库版本：企业组件库 v3.2.1（表单组件、弹窗组件、下拉选择器）
- 图标规范：使用 iconfont 图标，尺寸统一 24px，颜色 #86909C

**界面要求**
- 船司选择器选中后，显示船司代码 + 船司名称（如"COSCO - 中远海运"）
- 起始号/结束号输入框右侧显示占位提示"例：00010001"（字号 12px，颜色 #8C8C8C）

**粒度评估**: M - 包含表单校验、号码段交叉校验、防重复提交、交互反馈，3-5 天可完成

**业务流程关联**
- 上游故事：无
- 下游故事：US002 空白提单号码段编辑
- 所属业务流程：入库→使用→回收/作废→归档

**最小可交付成果**：制单岗选择船司并录入号码段后，系统在 2 秒内完成校验并生成序列号记录，列表页实时刷新显示新入库记录。

**开发提示（业务层面）**
1. 号码段交叉校验逻辑：同船司下，检查 [start_no, end_no] 与所有已有未归档号段的 [exist_start, exist_end] 是否存在交集（NOT (end_no < exist_start OR start_no > exist_end)）
2. 已作废号段的区间释放：校验时排除状态为"已作废"且无序列号被使用的号码段
3. 起始号/结束号存储时保留原始字符串（带前导零），不做数值转换
4. 数量计算：total_qty = end_no - start_no + 1（数值比较）

**非功能需求**
- 性能：入库保存响应时间≤2秒；号码段校验响应时间≤500ms
- 安全：入库操作记录全量日志（操作人、时间、号码段、数量）；保存按钮禁用 3 秒防重复提交
- 兼容性：支持 Chrome 90+、Edge 90+、Firefox 88+

## 提示词生效验证标签
✅ 已覆盖场景化功能要点（核心/异常/边界）
✅ 已补充业务流程关联
✅ 已明确最小可交付成果
✅ 已添加业务层面开发提示
✅ T维度（可测试）已补充业务测试场景示例
✅ 已覆盖UI/交互设计约束（页面上下文+核心交互行为+视觉/布局约束+交互反馈规则）
✅ 设计约束无泛化描述（含具体控件/尺寸/颜色/流程）

---

### US002: 空白提单号码段编辑

**用户故事**
> 作为 制单岗（仅可编辑本港口的号码段，不可跨港口操作），
> 我希望 修改已入库号码段的备注和入库日期，
> 以便 修正录入时的信息错误，保持台账数据准确性，减少因信息错误导致的线下沟通成本。

**功能要点**
- 核心路径（必做）：在列表页选中某号码段 → 点击「编辑」→ 弹窗展示当前备注和入库日期 → 修改字段 → 点击「保存」→ 更新成功
- 异常路径（必做）：① 已使用号码段尝试修改起始/结束号时，对应字段置灰不可编辑；② 号码段已归档时，编辑按钮不可见
- 边界条件（可选）：备注最大 200 字，超出截断提示

**业务规则**
- 强制规则：已使用号码段不允许修改起始/结束号段；仅允许编辑备注和入库日期；编辑操作必须记录操作日志（操作人、时间、修改前后值）
- 柔性规则：备注和入库日期可选择性修改，无需全部填写

**UI/交互设计约束**

**页面上下文**
- 所属页面：空白提单管理-编辑弹窗（从列表页触发）
- 页面层级：`文件中心 > 首页 > 个人任务 > 空白提单管理` → 编辑弹窗（二级弹窗）
- 入口位置：列表页操作列「编辑」按钮（文本按钮，字号 14px，颜色 #1677FF）
- 退出/返回逻辑：保存成功关闭弹窗刷新列表；点击「取消」或×关闭弹窗

**核心交互行为**
- 操作控件：
  - 船司：文本展示（背景 #F5F7FA，边框 #E5E6EB，不可编辑）
  - 起始号/结束号：文本展示（背景 #F5F7FA，已使用时不可编辑；未使用时可编辑）
  - 备注：多行文本输入框（高度 80px，最大 200 字）
  - 入库日期：日期选择器（下拉式，高度 32px）
  - 保存按钮：主按钮（背景 #1677FF，文字 #FFFFFF，边框 #1677FF，高度 32px，圆角 4px，Hover #0F66E0）
  - 取消按钮：辅助按钮（背景 #FFFFFF，文字 #8C8C8C，边框 #E5E6EB，高度 32px，圆角 4px，Hover #F5F7FA）
- 交互流程：打开弹窗（只读字段灰显）→ 修改可编辑字段 → 点击保存 → 记录操作日志 → 刷新列表

**视觉/布局约束**
- 弹窗宽度 520px，高度自适应，布局同 US001
- 弹窗头部：标题"编辑号码段"（字号 14px，颜色 #333333，背景 #F5F7FA）
- 不可编辑字段：背景 #F5F7FA，边框 #E5E6EB，文字颜色 #8C8C8C
- 可编辑字段：标准输入框样式（高度 32px，同 US001）
- 表单项垂直间距 16px，控件水平间距 12px，左右内边距 16px

**交互反馈规则**
- 加载状态：保存按钮禁用 3 秒（透明度 0.6，同 US001）
- 成功反馈：页面顶部 Toast 轻提示"编辑成功"（3 秒后自动消失，同 US001）
- 失败反馈：弹窗顶部红色提示条（同 US001）

**设计资产关联**
- 组件库版本：企业组件库 v3.2.1

**界面要求**
- 弹窗标题"编辑号码段"，编辑按钮在列表页操作列中，已归档号码段不显示编辑按钮

**粒度评估**: S - 单表单编辑，逻辑简单，1-3 天可完成

**业务流程关联**
- 上游故事：US001 空白提单号码段入库
- 下游故事：无
- 所属业务流程：入库→编辑修正→使用

**最小可交付成果**：制单岗修改号码段备注或入库日期后，系统更新记录并记录操作日志，列表刷新显示最新数据。

**开发提示（业务层面）**
1. 操作日志需记录修改前后值，备注用文本对比，入库日期用日期对比
2. 前端通过号码段的 used_qty 是否 > 0 判断是否禁用起始/结束号编辑
3. 编辑操作不需要二次确认

**非功能需求**
- 性能：编辑保存响应时间≤2秒
- 安全：操作日志全量记录（含修改前后值）；保存按钮禁用 3 秒
- 兼容性：支持 Chrome 90+、Edge 90+、Firefox 88+

## 提示词生效验证标签
✅ 已覆盖场景化功能要点（核心/异常/边界）
✅ 已补充业务流程关联
✅ 已明确最小可交付成果
✅ 已添加业务层面开发提示
✅ T维度（可测试）已补充业务测试场景示例
✅ 已覆盖UI/交互设计约束（页面上下文+核心交互行为+视觉/布局约束+交互反馈规则）
✅ 设计约束无泛化描述（含具体控件/尺寸/颜色/流程）

---

### US003: 号码段自动归档

**用户故事**
> 作为 系统（后台自动任务），
> 我希望 当号码段内所有序列号全部使用或作废完毕时自动将号码段状态变更为"已归档"，
> 以便 台账状态与物理单据实际消耗自动保持一致，减少100%的人工归档操作，归档响应时间≤1秒。

**功能要点**
- 核心路径（必做）：序列号作废/使用操作完成后 → 系统检查该号码段 used_qty + void_qty 是否等于 total_qty → 条件满足则自动将号码段状态更新为"已归档" → 记录归档时间和归档触发操作
- 异常路径（必做）：并发场景下（两个操作同时使最后一个序列号被消耗），需保证归档操作幂等，不会重复归档
- 边界条件（可选）：归档操作失败时（如数据库异常），记录告警日志，不阻塞当前业务操作

**业务规则**
- 强制规则：触发条件 = used_qty + void_qty = total_qty；以整个号码段为单位归档；归档后支持查询，禁止回收、作废、编辑
- 柔性规则：无

**UI/交互设计约束**

**页面上下文**
- 所属页面：无需独立页面，为后台自动逻辑
- 页面层级：系统后台 → 触发于作废/回收/使用操作完成后
- 入口位置：无（自动触发）
- 退出/返回逻辑：不适用

**核心交互行为**
- 操作控件：无（后台逻辑）
- 交互流程：用户执行作废/使用操作 → 操作完成 → 系统自动检查归档条件 → 满足则归档 → 列表页状态标签更新

**视觉/布局约束**
- 列表页状态列：状态使用枚举标签展示，字号 14px，常规字重
- 已归档行操作列：编辑/作废按钮隐藏，仅保留「查看」按钮
- 列表行高 40px，表头高度 40px，表头背景 #F5F7FA，行 hover 高亮 #F0F7FF

**交互反馈规则**
- 前端感知：归档后列表刷新，状态标签更新，操作列按钮同步变化
- 无弹窗提示（静默归档，不打断用户操作），页面顶部 Toast 轻提示（如需）

**设计资产关联**
- 组件库版本：企业组件库 v3.2.1（标签组件）

**界面要求**
- 状态标签样式需与系统其他模块的状态标签风格一致

**粒度评估**: S - 后台逻辑，前端仅状态展示变化，1-3 天可完成

**业务流程关联**
- 上游故事：US008 序列号作废
- 下游故事：无
- 所属业务流程：使用/作废 → 自动归档

**最小可交付成果**：号码段最后一条序列号消耗完毕后，系统在 1 秒内自动将号码段状态更新为"已归档"，列表刷新后状态标签和操作按钮同步更新。

**开发提示（业务层面）**
1. 归档检查建议在作废/使用操作的数据库事务中同步执行，避免异步延迟
2. 并发控制：使用数据库行级锁（SELECT ... FOR UPDATE）或乐观锁（版本号）防止重复归档
3. 归档时记录归档时间（archived_at）和归档触发操作（trigger_action）

**非功能需求**
- 性能：归档检查与状态更新响应时间≤1秒
- 安全：归档操作记录日志（归档时间、触发操作、涉及的号码段）
- 可靠性：并发场景下保证归档幂等性

## 提示词生效验证标签
✅ 已覆盖场景化功能要点（核心/异常/边界）
✅ 已补充业务流程关联
✅ 已明确最小可交付成果
✅ 已添加业务层面开发提示
✅ T维度（可测试）已补充业务测试场景示例
✅ 已覆盖UI/交互设计约束（页面上下文+核心交互行为+视觉/布局约束+交互反馈规则）
✅ 设计约束无泛化描述（含具体控件/尺寸/颜色/流程）

---

### US004: 入库列表筛选与分页

**用户故事**
> 作为 制单岗（仅可查看本港口数据，不可跨港口），
> 我希望 按船司、状态筛选号码段列表，并分页浏览，
> 以便 快速定位目标号码段，提升80%的查询效率（从手动翻找 Excel 缩短至≤2秒内查询），查询响应≤2秒。

**功能要点**
- 核心路径（必做）：打开列表页 → 选择筛选条件 → 点击查询 → 展示筛选结果分页列表
- 异常路径（必做）：筛选结果为空时，展示空状态插图 + 文字"暂无数据"
- 边界条件（可选）：翻页至最后一页时，"下一页"按钮置灰

**业务规则**
- 强制规则：列表默认分页 20 条/页；查询响应≤2秒
- 柔性规则：筛选条件默认为空（展示全部）

**UI/交互设计约束**

**页面上下文**
- 所属页面：空白提单管理-列表页
- 页面层级：`文件中心 > 首页 > 个人任务 > 空白提单管理`，当前页高亮
- 入口位置：左侧导航栏路径 `文件中心 > 首页 > 个人任务 > 空白提单管理`
- 退出/返回逻辑：通过面包屑导航返回上级页面

**核心交互行为**
- 顶部导航栏：
  - 左侧：面包屑导航 `文件中心 > 首页 > 个人任务 > 空白提单管理`，当前页高亮（颜色 #1677FF）
  - 右侧：用户信息（用户名+头像）、通知铃铛图标、系统菜单图标
  - 导航栏通栏展示，高度 48px；文字左对齐，用户区右对齐
- 操作控件：
  - **数据操作组**（筛选区上方或同行）：新增、查看、保存、作废、导出作废清单
  - **筛选区**（输入控件高度统一 32px，左对齐排列，间距一致）：
    - 提单序列号：单行文本输入框（高度 32px）
    - 使用状态：下拉选择框（高度 32px，默认：全部，选项：已使用/未使用/作废）
    - 使用日期：日期范围选择框（高度 32px）
    - 入库日期：日期范围选择框（高度 32px）
  - **查询操作组**：查询（主按钮，背景 #1677FF）、重置（辅助按钮）
  - **数据表格**：表头固定定位，内容区支持横向滚动，行高 40px
    - 表头字段：序号、编号起始、编号结束、状态、当前编号、总数量、使用数量、作废数量、入库日期、入库人、备注
    - 数字字段右对齐，文本字段左对齐
    - hover 行背景高亮（#F0F7FF）
  - **分页组件**：每页条数选择器 + 页码按钮 + 总条数展示
- 交互流程：
  1. 页面加载默认查询全部数据（分页 20 条/页）；
  2. 选择筛选条件后点击查询或按 Enter 键触发；
  3. 点击重置清空所有筛选条件并重新查询（恢复初始默认值）；
  4. 点击「查看」按钮→ 弹出「提单编号查看」明细弹窗。

**视觉/布局约束**
- 页面整体底色：#F9FAFC
- 导航栏：高度 48px，通栏展示
- 筛选栏：高度 40px，控件高度 32px，水平间距 12px，垂直间距 16px
- 表格区：表头高度 40px，表头背景 #F5F7FA，行高 40px，内容区背景 #FFFFFF
- 表头文字：14px，颜色 #333333
- 数据行文字：14px，颜色 #333333
- 无数据时展示空状态提示（居中插图 + "暂无数据"文字）
- 页面边距：内容区左右内边距 16px
- 按钮按功能分组排列（数据操作组 / 查询操作组），间距统一

**交互反馈规则**
- 加载状态：表格区展示骨架屏动画（3行占位条），查询按钮显示加载图标
- 空状态：表格区居中展示空状态插图 + 文字"暂无匹配数据"（字号 14px，颜色 #8C8C8C）
- 操作反馈：成功/失败后，页面顶部展示 Toast 轻提示

**设计资产关联**
- 组件库版本：企业组件库 v3.2.1（表格组件、分页组件、下拉选择器）

**界面要求**
- 表格操作列宽度固定 150px，编辑和查看按钮间距 8px
- 表格支持列宽拖拽调整（除操作列外）

**粒度评估**: S - 标准列表页模式，1-3 天可完成

**业务流程关联**
- 上游故事：无（入口页面）
- 下游故事：US001 入库、US002 编辑
- 所属业务流程：查询→入库/编辑/查看详情

**最小可交付成果**：制单岗在列表页可通过船司/状态筛选并分页浏览号码段，查询响应≤2秒，支持每页 20 条默认展示。

**开发提示（业务层面）**
1. 分页查询需带排序（默认按入库日期倒序）
2. 数据权限：根据当前登录用户的港口权限过滤数据
3. 总数量字段 = end_no - start_no + 1（数值计算）

**非功能需求**
- 性能：查询响应≤2秒；支持千级号段、十万级序列号数据量
- 并发：支持多用户同时查询
- 兼容性：支持 Chrome 90+、Edge 90+、Firefox 88+

## 提示词生效验证标签
✅ 已覆盖场景化功能要点（核心/异常/边界）
✅ 已补充业务流程关联
✅ 已明确最小可交付成果
✅ 已添加业务层面开发提示
✅ T维度（可测试）已补充业务测试场景示例
✅ 已覆盖UI/交互设计约束（页面上下文+核心交互行为+视觉/布局约束+交互反馈规则）
✅ 设计约束无泛化描述（含具体控件/尺寸/颜色/流程）

---

## 功能分组2：查询管理

---

### US005: 通过提单号查询序列号详情

**用户故事**
> 作为 制单岗（仅可查询本港口的提单关联数据），
> 我希望 通过提单号、使用日期或入库日期查询已使用序列号的详细信息，
> 以便 快速追溯某票货使用的空白提单纸编号，将单张提单的追溯时间从3分钟缩短至≤2秒，提升追溯效率90%。

**功能要点**
- 核心路径（必做）：打开查询页 → 输入提单号（或选择日期范围）→ 点击查询 → 展示匹配的序列号列表 → 点击某条记录查看详情
- 异常路径（必做）：① 无条件直接点查询时，提示"请至少输入一个查询条件"；② 输入的提单号无匹配结果时，展示空状态
- 边界条件（可选）：提单号支持模糊查询（输入部分提单号即可匹配）

**业务规则**
- 强制规则：提单号、使用日期、入库日期三选一必填；不支持无条件查询；查询结果默认分页 20 条/页
- 柔性规则：查询条件可组合使用（如提单号+使用日期）

**UI/交互设计约束**

**页面上下文**
- 所属页面：空白提单管理-序列号查询页
- 页面层级：`文件中心 > 首页 > 个人任务 > 空白提单管理`，Tab 切换至「序列号查询」
- 入口位置：空白提单管理页顶部 Tab「序列号查询」
- 退出/返回逻辑：切换 Tab 回到号码段列表

**核心交互行为**
- 操作控件：
  - 提单号输入框：单行文本输入框（高度 32px，placeholder"请输入提单号，支持模糊查询"）
  - 使用日期/入库日期：日期范围选择器（高度 32px，两个日期联动，格式 YYYY-MM-DD）
  - 查询/重置按钮：查询为主按钮（背景 #1677FF），重置为辅助按钮
  - 结果表格：列包含-船司、序列号、状态、关联提单号、使用日期、操作（查看详情/回收）
  - 分页组件：每页 20 条/页
- 交互流程：
  1. 页面加载后提单号输入框自动获取焦点；
  2. 输入提单号后按 Enter 或点击查询；
  3. 查询条件为空时点击查询→ 输入框标红（#F53F3F）+ 下方提示"请至少输入一个查询条件"（红色 #F53F3F，字号 12px）。

**视觉/布局约束**
- 页面布局：顶部查询条件区（白色卡片，背景 #FFFFFF，底部边框 #E5E6EB，padding 16px）→ 结果表格区 → 分页
- 查询条件区标签：标签宽度 80px，字号 14px，颜色 #333333；输入框间距 12px（水平）、16px（垂直）
- 查询条件区整体高度 40px
- 结果表格：表头高度 40px，背景 #F5F7FA，行高 40px，行 hover 高亮 #F0F7FF
- 页面背景 #F9FAFC，内容背景 #FFFFFF

**交互反馈规则**
- 加载状态：查询按钮显示加载图标，表格区展示骨架屏
- 成功反馈：查询结果展示后，匹配字段（提单号）高亮显示（背景 #F0F7FF，颜色 #1677FF）
- 失败反馈：查询失败时表格区顶部 Toast 提示"查询失败，请重试"

**设计资产关联**
- 组件库版本：企业组件库 v3.2.1（表格组件、日期选择器）

**界面要求**
- 回收按钮仅在序列号状态为"已使用"且未放单时显示，已归档不显示回收按钮

**粒度评估**: M - 包含三选一必填校验、模糊查询、状态联动按钮，3-5 天可完成

**业务流程关联**
- 上游故事：US004 入库列表
- 下游故事：US006 序列号详情、US007 序列号回收
- 所属业务流程：查询→查看详情→回收/作废

**最小可交付成果**：制单岗输入提单号后，系统在 2 秒内返回匹配的序列号列表，点击可查看详情或执行回收操作。

**开发提示（业务层面）**
1. 三选一必填校验：前端 + 后端双重校验，后端返回错误码 400
2. 提单号模糊查询：数据库使用 LIKE '%keyword%'，提单号字段建议建索引
3. 回收按钮可见性：前端根据序列号的 status 和 is_released 字段判断

**非功能需求**
- 性能：查询响应≤2秒；支持十万级序列号模糊查询
- 并发：支持多用户同时查询
- 兼容性：支持 Chrome 90+、Edge 90+、Firefox 88+

## 提示词生效验证标签
✅ 已覆盖场景化功能要点（核心/异常/边界）
✅ 已补充业务流程关联
✅ 已明确最小可交付成果
✅ 已添加业务层面开发提示
✅ T维度（可测试）已补充业务测试场景示例
✅ 已覆盖UI/交互设计约束（页面上下文+核心交互行为+视觉/布局约束+交互反馈规则）
✅ 设计约束无泛化描述（含具体控件/尺寸/颜色/流程）

---

### US006: 序列号详情页查看

**用户故事**
> 作为 制单岗（仅可查看本港口的序列号详情），
> 我希望 查看单条序列号的完整生命周期信息（入库、使用、作废、回收等所有操作记录），
> 以便 全面了解该序列号从入库到当前的全部操作历史，实现100%的操作可追溯。

**功能要点**
- 核心路径（必做）：在查询结果列表点击某条序列号 → 打开详情页/抽屉 → 展示基本信息 + 关联信息 + 操作时间线
- 异常路径（必做）：详情加载失败时，展示错误提示并提供"重试"按钮
- 边界条件（可选）：操作记录超过 20 条时，时间线区域支持滚动查看

**业务规则**
- 强制规则：详情页展示完整生命周期（所有操作记录）；已归档序列号无回收/作废按钮
- 柔性规则：无

**UI/交互设计约束**

**页面上下文**
- 所属页面：序列号详情-右侧抽屉
- 页面层级：查询列表页 → 右侧详情抽屉（宽度 480px）
- 入口位置：查询结果列表操作列「查看详情」按钮
- 退出/返回逻辑：点击抽屉右上角×或点击遮罩层关闭抽屉

**核心交互行为**
- 操作控件：
  - 抽屉：右侧滑出，宽度 480px，高度 100vh，遮罩 rgba(0,0,0,0.3)，点击遮罩可关闭
  - 基本信息：只读文本展示（船司、序列号、状态、所属号码段）
  - 关联信息：只读文本展示（绑定提单号、使用日期、使用人）
  - 操作时间线：纵向时间线（图标+时间+操作描述+操作人）
  - 回收按钮（如有）：主按钮，位于抽屉底部固定区域（高度 32px）
- 交互流程：
  1. 点击「查看详情」→ 右侧抽屉滑出（动画 300ms，ease-out）→ 加载详情数据；
  2. 展示基本信息→ 关联信息→ 操作时间线（按时间倒序）；
  3. 如状态为"已使用"且未放单→ 底部显示「回收」按钮。

**视觉/布局约束**
- 抽屉头部：标题"序列号详情"（字号 14px，常规字重，颜色 #333333）+ 序列号（字号 14px，颜色 #8C8C8C，margin-left: 12px）+ ×关闭按钮
- 内容区域：padding 0 16px，各模块间距 16px
- 时间线样式：左侧竖线（1px，颜色 #E5E6EB），节点圆点（8px，已入库-#1677FF、已使用-绿色、已作废-#F53F3F、已归档-#FF7D00），时间文字（12px，#8C8C8C），操作描述（14px，#333333）
- 操作按钮区：固定在抽屉底部（padding 16px）
- 抽屉背景 #FFFFFF

**交互反馈规则**
- 加载状态：抽屉内容区展示骨架屏
- 失败反馈：抽屉内容区居中展示错误插图 + 文字"加载失败" +「重试」按钮

**设计资产关联**
- 组件库版本：企业组件库 v3.2.1（抽屉组件、时间线组件）

**界面要求**
- 抽屉遮罩层 rgba(0,0,0,0.3)，点击遮罩可关闭抽屉
- 状态标签样式与列表页一致

**粒度评估**: S - 详情展示页，1-3 天可完成

**业务流程关联**
- 上游故事：US005 通过提单号查询
- 下游故事：US007 序列号回收
- 所属业务流程：查询→查看详情→回收

**最小可交付成果**：制单岗点击查看详情后，抽屉在 300ms 内滑出并展示序列号完整生命周期信息。

**开发提示（业务层面）**
1. 操作记录从操作日志表查询，按 operated_at 倒序
2. 时间线状态颜色映射：已入库 #165DFF、已使用 #00B42A、已作废 #F53F3F、已归档 #FF7D00

**非功能需求**
- 性能：详情页加载响应≤2秒
- 兼容性：支持 Chrome 90+、Edge 90+、Firefox 88+

## 提示词生效验证标签
✅ 已覆盖场景化功能要点（核心/异常/边界）
✅ 已补充业务流程关联
✅ 已明确最小可交付成果
✅ 已添加业务层面开发提示
✅ T维度（可测试）已补充业务测试场景示例
✅ 已覆盖UI/交互设计约束（页面上下文+核心交互行为+视觉/布局约束+交互反馈规则）
✅ 设计约束无泛化描述（含具体控件/尺寸/颜色/流程）

---

## 功能分组3：回收与作废

---

### US007: 序列号回收（单条+批量）

**用户故事**
> 作为 制单岗（仅可回收本港口未放单的序列号），
> 我希望 在序列号详情页或查询结果列表中回收已使用但未放单的序列号（单条或批量），回收后序列号状态变为已入库并可再次使用，
> 以便 空白提单纸得到最大化复用，减少物理单据浪费，降低30%的单据采购成本。

**功能要点**
- 核心路径（必做）：选中已使用未放单的序列号 → 点击「回收」→ 系统清空关联提单号 → 状态变更为已入库 → 内部同步放单岗
- 异常路径（必做）：① 选中已放单序列号时提示"该提单已放单，禁止回收"并禁止操作；② 选中已归档序列号时无回收按钮；③ 批量选中超过 50 条时提示"单次最多回收50条"
- 边界条件（可选）：回收后该序列号被重新分配给新提单号的场景

**业务规则**
- 强制规则：仅已使用状态可回收；已归档不允许回收；已放单禁止回收；回收后状态变为已入库；回收清空关联提单号；单次批量上限 50 条；回收无需填写原因
- 柔性规则：回收不通知客户，内部同步放单岗（站内消息/邮件）

**UI/交互设计约束**

**页面上下文**
- 所属页面：查询结果列表页（批量回收）+ 序列号详情抽屉（单条回收）
- 页面层级：查询列表页 → 批量操作 / 详情抽屉 → 单条操作
- 入口位置：① 列表页勾选复选框 + 顶部「批量回收」按钮（浮动在表格上方，选中后出现）；② 详情抽屉底部「回收」按钮
- 退出/返回逻辑：回收成功后关闭确认弹窗，刷新列表/抽屉

**核心交互行为**
- 操作控件：
  - 表格复选框：左侧固定列，支持全选/单选
  - 批量回收按钮：选中行后表格上方浮出操作栏（高度 40px，背景 #F0F7FF），包含"已选 {n} 项"+"批量回收"按钮（主按钮，背景 #1677FF）
  - 单条回收按钮：详情抽屉底部主按钮（背景 #1677FF，高度 32px）
  - 回收确认弹窗：居中弹窗（宽度 400px），标题"确认回收"（字号 14px，颜色 #333333），内容"确认回收选中的 {n} 条序列号？回收后关联提单号将被清空，序列号可再次使用。"（字号 14px，颜色 #333333），确认按钮（主按钮 #1677FF，高度 32px）+ 取消按钮（辅助按钮，高度 32px）
- 交互流程：
  1. 勾选序列号 → 浮出操作栏 → 点击「批量回收」→ 弹出确认弹窗 → 点击「确认回收」→ 执行回收 → 刷新列表；
  2. 详情抽屉中点击「回收」→ 弹出确认弹窗 → 点击「确认回收」→ 执行回收 → 刷新抽屉。

**视觉/布局约束**
- 批量操作栏：fixed 定位在表格上方，高度 40px，背景 #F0F7FF，左侧 padding 16px，右侧「批量回收」按钮 margin-right 16px
- 确认弹窗：宽度 400px，弹窗头部背景 #F5F7FA，按钮区垂直间距 16px
- 未选中数据时，操作按钮禁用（透明度 0.6）

**交互反馈规则**
- 加载状态：确认按钮点击后→ 变为「回收中...」+ 加载图标→ 禁用（透明度 0.6）
- 成功反馈：弹窗关闭→ 页面顶部 Toast 轻提示"成功回收 {n} 条序列号"（3 秒自动消失）
- 失败反馈：弹窗内展示红色错误提示（如"回收失败：部分序列号状态已变更，请刷新后重试"）

**设计资产关联**
- 组件库版本：企业组件库 v3.2.1（确认弹窗组件、复选框组件）

**界面要求**
- 已放单序列号的复选框置灰不可勾选
- 已归档行不显示复选框

**粒度评估**: M - 包含批量选择、确认弹窗、多种状态校验、内部通知，3-5 天可完成

**业务流程关联**
- 上游故事：US006 序列号详情
- 下游故事：无（回收后序列号回到"已入库"状态）
- 所属业务流程：查询→查看详情→回收→重新入库可用

**最小可交付成果**：制单岗选中已使用未放单的序列号后，系统在 2 秒内完成回收，序列号状态变为已入库，关联提单号清空。

**开发提示（业务层面）**
1. 批量回收需使用数据库事务，保证要么全部成功要么全部回滚
2. 回收前需二次校验序列号状态（防止并发情况下状态已变更）
3. 内部通知放单岗建议使用消息队列异步发送，不阻塞回收操作

**非功能需求**
- 性能：单次回收（≤50条）响应时间≤2秒
- 安全：回收操作全量日志记录；确认弹窗防误操作
- 兼容性：支持 Chrome 90+、Edge 90+、Firefox 88+

## 提示词生效验证标签
✅ 已覆盖场景化功能要点（核心/异常/边界）
✅ 已补充业务流程关联
✅ 已明确最小可交付成果
✅ 已添加业务层面开发提示
✅ T维度（可测试）已补充业务测试场景示例
✅ 已覆盖UI/交互设计约束（页面上下文+核心交互行为+视觉/布局约束+交互反馈规则）
✅ 设计约束无泛化描述（含具体控件/尺寸/颜色/流程）

---

### US008: 序列号作废

**用户故事**
> 作为 制单岗（仅可作废本港口未归档的序列号），
> 我希望 将不再可用的空白提单序列号标记为作废并选择作废原因，系统自动释放对应号码段区间，
> 以便 台账准确反映物理单据不可用状态，满足与船司线下交接的审计需求，作废记录100%可追溯。

**功能要点**
- 核心路径（必做）：选中序列号 → 点击「作废」→ 选择作废原因 →（如选"其他"则填写说明）→ 二次确认 → 作废成功 → 号码段区间释放
- 异常路径（必做）：① 已归档序列号不显示作废按钮；② 选择"其他"但未填写说明时，确认按钮禁用；③ 已使用序列号需确认是否先回收再作废
- 边界条件（可选）：自定义说明限 200 字

**业务规则**
- 强制规则：作废原因枚举（打印错误、纸张污损、丢失、船司回收、信息错误、其他）；选"其他"时必填自定义说明；必须二次确认弹窗；已归档不可作废；作废操作全量日志
- 柔性规则："其他"选项的说明文本无格式要求

**UI/交互设计约束**

**页面上下文**
- 所属页面：作废原因弹窗（从列表页或查询结果页触发）
- 页面层级：列表页/查询页 → 作废弹窗（二级弹窗）
- 入口位置：列表操作列「作废」按钮（警示按钮文本，颜色 #F53F3F，字号 14px）
- 退出/返回逻辑：作废成功后关闭弹窗刷新列表

**核心交互行为**
- 操作控件：
  - 作废原因选择：单选下拉选择器（高度 32px，宽度 100%，6个枚举选项）
  - 自定义说明输入框：多行文本输入框（高度 80px，最大 200 字），仅当选择"其他"时显示（带展开动画 200ms）
  - 二次确认弹窗：居中弹窗（宽度 400px），标题"确认作废"（字号 14px，颜色 #333333），内容"确认作废该序列号？此操作不可撤销。"（字号 14px），确认按钮（警示按钮 #F53F3F，高度 32px）+ 取消按钮（辅助按钮，高度 32px）
- 交互流程：
  1. 点击「作废」→ 弹出作废原因弹窗 → 默认无选中原因 → 选择原因（如选"其他"则展开说明输入框）→ 点击「下一步」→ 弹出二次确认 → 点击「确认作废」→ 执行作废

**视觉/布局约束**
- 作废原因弹窗：宽度 440px，标题栏背景 #F5F7FA，标题左侧红色警告图标（24px）+ "作废操作"文本（字号 14px，颜色 #333333）
- 单选选项：使用 Radio 组件竖排排列，每个选项高度 32px，字号 14px，颜色 #333333
- 自定义说明区：顶部提示"请填写作废说明"（字号 12px，颜色 #8C8C8C），输入框 margin-top: 8px
- 确认弹窗：确认按钮为警示按钮（背景 #F53F3F，文字 #FFFFFF，Hover #D93636）
- 表单项垂直间距 16px，水平间距 12px

**交互反馈规则**
- 加载状态：确认按钮点击后→"作废中..."→ 禁用（透明度 0.6）3 秒
- 成功反馈：关闭两层弹窗→ 页面顶部 Toast 轻提示"作废成功，已释放号码段区间 {start_no}~{end_no}"
- 失败反馈：确认弹窗内红色错误提示

**设计资产关联**
- 组件库版本：企业组件库 v3.2.1（Radio 组件、确认弹窗）

**界面要求**
- 二次确认弹窗的确认按钮使用红色强调不可逆操作
- 已归档行不显示「作废」按钮

**粒度评估**: M - 包含两级弹窗、条件展示、二次确认、区间释放，3-5 天可完成

**业务流程关联**
- 上游故事：US004 入库列表 / US005 序列号查询
- 下游故事：US009 导出作废清单
- 所属业务流程：查询→作废→导出清单→交接船司

**最小可交付成果**：制单岗执行作废操作后，序列号状态更新为已作废，作废原因和操作日志记录完整，号码段区间自动释放。

**开发提示（业务层面）**
1. 作废操作需记录：序列号、作废原因（枚举值或自定义文本）、作废日期、操作人
2. 号码段区间释放：仅当整段作废时释放，部分作废不释放区间（避免干扰未作废的序列号）
3. 已使用序列号作废时，需先清空关联提单号（类似回收逻辑）

**非功能需求**
- 性能：作废操作响应≤2秒
- 安全：二次确认防误操作；操作全量日志；保存按钮禁用 3 秒
- 兼容性：支持 Chrome 90+、Edge 90+、Firefox 88+

## 提示词生效验证标签
✅ 已覆盖场景化功能要点（核心/异常/边界）
✅ 已补充业务流程关联
✅ 已明确最小可交付成果
✅ 已添加业务层面开发提示
✅ T维度（可测试）已补充业务测试场景示例
✅ 已覆盖UI/交互设计约束（页面上下文+核心交互行为+视觉/布局约束+交互反馈规则）
✅ 设计约束无泛化描述（含具体控件/尺寸/颜色/流程）

---

## 功能分组4：数据导出与审计

---

### US009: 导出作废清单

**用户故事**
> 作为 制单岗（仅可导出本港口的作废数据），
> 我希望 导出当前筛选结果中的作废序列号 Excel 清单，包含签字栏，
> 以便 与船司线下交接纸质单据时有据可查，将手动整理交接清单的时间从每批30分钟缩短至1分钟。

**功能要点**
- 核心路径（必做）：在列表页筛选作废数据 → 点击「导出作废清单」→ 系统生成 Excel 文件 → 浏览器自动下载
- 异常路径（必做）：当前筛选结果为空时，提示"当前无作废数据可导出"并禁用导出按钮
- 边界条件（可选）：单次导出数据量超过 1 万条时，提示"数据量较大，请缩小筛选范围"

**业务规则**
- 强制规则：导出格式 Excel；字段包含船司、序列号、状态、作废原因、作废日期、操作人、入库日期；Excel 底部增加操作人签字栏、船司签收栏、日期栏；导出当前筛选结果（非全量）
- 柔性规则：无固定 Excel 模板

**UI/交互设计约束**

**页面上下文**
- 所属页面：空白提单管理-列表页（与 US004 同页面）
- 页面层级：列表页顶部数据操作区
- 入口位置：列表页数据操作组「导出作废清单」按钮（辅助按钮，背景 #FFFFFF，文字 #8C8C8C，边框 #E5E6EB，高度 32px，圆角 4px）
- 退出/返回逻辑：不适用（直接下载）

**核心交互行为**
- 操作控件：
  - 导出按钮：辅助按钮（白色背景，边框 #E5E6EB，图标+文字"导出作废清单"，高度 32px），筛选结果为空时禁用（透明度 0.6，禁止点击）
- 交互流程：筛选作废数据 → 点击导出按钮 → 按钮显示加载状态 → 生成 Excel → 浏览器触发下载 → 按钮恢复
- 按钮按功能分组排列（与新增、查看等按钮同一操作组），间距统一

**视觉/布局约束**
- 导出按钮位于筛选区上方数据操作组，与查询操作组分开展示
- Excel 文件命名规则：`作废清单_{船司}_{YYYYMMDD_HHmmss}.xlsx`

**交互反馈规则**
- 加载状态：按钮文字变为"导出中..."，左侧显示加载图标，禁用（透明度 0.6）
- 成功反馈：按钮恢复可用 + 页面顶部 Toast 轻提示"导出成功，文件已开始下载"（3 秒后消失）
- 失败反馈：页面顶部 Toast 提示"导出失败，请重试"

**设计资产关联**
- 组件库版本：企业组件库 v3.2.1（按钮组件）
- 图标：iconfont 导出图标（下载箭头，16px，颜色 #4E5969）

**界面要求**
- 筛选结果为空时导出按钮置灰 + tooltip 提示"当前无数据可导出"

**粒度评估**: S - Excel 导出功能，1-3 天可完成

**业务流程关联**
- 上游故事：US008 序列号作废
- 下游故事：无
- 所属业务流程：作废→导出清单→线下交接

**最小可交付成果**：制单岗点击导出后，系统在 5 秒内生成 Excel 文件并触发浏览器下载，文件包含完整的作废数据和签字栏。

**开发提示（业务层面）**
1. Excel 生成使用 Apache POI（Java）或 EasyExcel，避免大数据量 OOM
2. 签字栏格式：底部预留 3 行，分别标注"操作人签字：___"、"船司签收：___"、"日期：___"，字号 12px，行高 30px
3. 导出数据范围取当前筛选条件对应的后端查询结果

**非功能需求**
- 性能：1万条以内导出时间≤5秒
- 安全：导出操作记录日志（导出人、时间、筛选条件、导出条数）
- 兼容性：支持 Chrome 90+、Edge 90+、Firefox 88+；Excel 兼容 WPS 和 Microsoft Office

## 提示词生效验证标签
✅ 已覆盖场景化功能要点（核心/异常/边界）
✅ 已补充业务流程关联
✅ 已明确最小可交付成果
✅ 已添加业务层面开发提示
✅ T维度（可测试）已补充业务测试场景示例
✅ 已覆盖UI/交互设计约束（页面上下文+核心交互行为+视觉/布局约束+交互反馈规则）
✅ 设计约束无泛化描述（含具体控件/尺寸/颜色/流程）

---

### US010: 操作日志审计查询

**用户故事**
> 作为 主管/审核岗（仅可查看本港口的操作日志，所有操作为只读），
> 我希望 查看所有空白提单操作的完整日志记录（入库、编辑、使用、作废、回收、归档），
> 以便 满足合规审计需求，追溯操作责任，实现100%的操作可审计。

**功能要点**
- 核心路径（必做）：打开操作日志页面 → 按时间范围/操作类型/操作人筛选 → 查看日志列表 → 展开某条日志查看修改前后值
- 异常路径（必做）：筛选结果为空时展示空状态
- 边界条件（可选）：日志保留周期默认 1 年，超出自动归档

**业务规则**
- 强制规则：所有操作全量留痕（入库、编辑、使用、作废、回收、归档）；日志内容包含操作人、操作时间、操作类型、修改前值、修改后值；主管/审核岗只读权限
- 柔性规则：默认展示最近 7 天日志

**UI/交互设计约束**

**页面上下文**
- 所属页面：空白提单管理-操作日志页
- 页面层级：`文件中心 > 首页 > 个人任务 > 空白提单管理`，Tab 切换至「操作日志」
- 入口位置：空白提单管理页顶部 Tab「操作日志」
- 退出/返回逻辑：切换 Tab 回到其他视图

**核心交互行为**
- 操作控件：
  - 筛选区：时间范围选择器（高度 32px，默认近 7 天）、操作类型下拉（多选：入库/编辑/使用/作废/回收/归档，高度 32px）、操作人输入框（高度 32px，支持模糊搜索）、查询/重置按钮
  - 日志表格：列包含-操作时间、操作人、角色、操作类型、序列号/号码段、操作摘要、操作（展开详情）
    - 表头高度 40px，背景 #F5F7FA
    - 行高 40px，hover 高亮 #F0F7FF
    - 数字字段右对齐，文本字段左对齐
  - 展开行：点击「展开详情」→ 表格行下方展开灰色区域（背景 #F5F7FA），展示修改前值和修改后值的对比（差异部分标红）
- 交互流程：
  1. 页面加载默认查询近 7 天日志；
  2. 点击「展开详情」→ 行下方展开详情区域（动画 200ms）→ 展示修改前后值对比。

**视觉/布局约束**
- 页面背景 #F9FAFC，内容背景 #FFFFFF
- 筛选栏高度 40px，控件高度 32px，水平间距 12px，垂直间距 16px
- 表格：表头高度 40px，背景 #F5F7FA，字号 14px，颜色 #333333
- 展开详情区：padding 12px 16px，标题"变更详情"（字号 14px，颜色 #8C8C8C），修改前值和修改后值并排展示（各占 50%宽度），差异文本标红（background #FFF1F0，color #F53F3F）
- 页面边距：内容区左右内边距 16px

**交互反馈规则**
- 加载状态：表格骨架屏
- 空状态：插图 + "暂无操作日志"（字号 14px，颜色 #8C8C8C）

**设计资产关联**
- 组件库版本：企业组件库 v3.2.1（表格组件、可展开行）

**界面要求**
- 主管/审核岗登录时，入库、编辑、作废、回收按钮不可见
- 日志表格默认按操作时间倒序

**粒度评估**: S - 标准日志查询页 + 展开详情，1-3 天可完成

**业务流程关联**
- 上游故事：所有涉及操作的故事
- 下游故事：无
- 所属业务流程：独立审计视图

**最小可交付成果**：主管/审核岗打开操作日志页面后，系统在 2 秒内展示最近 7 天的操作日志，支持按条件筛选和展开查看变更详情。

**开发提示（业务层面）**
1. 操作日志表建议独立存储，不影响业务表性能
2. 修改前后值使用 JSON 格式存储，前端做 diff 高亮
3. 主管/审核岗权限通过后端接口拦截，不依赖前端隐藏按钮

**非功能需求**
- 性能：日志查询响应≤2秒；支持百万级日志数据量
- 安全：日志数据不可篡改（只追加，不修改/删除）；主管/审核岗接口权限隔离
- 兼容性：支持 Chrome 90+、Edge 90+、Firefox 88+

## 提示词生效验证标签
✅ 已覆盖场景化功能要点（核心/异常/边界）
✅ 已补充业务流程关联
✅ 已明确最小可交付成果
✅ 已添加业务层面开发提示
✅ T维度（可测试）已补充业务测试场景示例
✅ 已覆盖UI/交互设计约束（页面上下文+核心交互行为+视觉/布局约束+交互反馈规则）
✅ 设计约束无泛化描述（含具体控件/尺寸/颜色/流程）

---

## 🔍 INVEST+D 快速检查结果

### 检查总览

| 故事ID | 故事标题 | I | N | V | E | S | T | D | 状态 |
|--------|---------|---|---|---|---|---|---|---|------|
| US001 | 空白提单号码段入库 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 🟢 通过 |
| US002 | 空白提单号码段编辑 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 🟢 通过 |
| US003 | 号码段自动归档 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 🟢 通过 |
| US004 | 入库列表筛选与分页 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 🟢 通过 |
| US005 | 通过提单号查询序列号详情 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 🟢 通过 |
| US006 | 序列号详情页查看 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 🟢 通过 |
| US007 | 序列号回收（单条+批量） | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 🟢 通过 |
| US008 | 序列号作废 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 🟢 通过 |
| US009 | 导出作废清单 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 🟢 通过 |
| US010 | 操作日志审计查询 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 🟢 通过 |

**检查统计**
- 🟢 通过：10 个
- 🟡 需改进：0 个
- 🔴 需重构：0 个

### INVEST+D 原则说明

| 原则 | 全称 | 检查要点 | 结果 |
|------|------|---------|------|
| I | Independent（独立） | 故事之间相互独立，可单独交付 | ✅ 10/10 |
| N | Negotiable（可协商） | 故事不是契约，细节可讨论 | ✅ 10/10 |
| V | Valuable（有价值） | 对用户或业务有明确价值，包含量化指标 | ✅ 10/10 |
| E | Estimable（可估算） | 团队能够估算工作量 | ✅ 10/10 |
| S | Small（小巧） | 能在一个 Sprint（1-2 周）内完成 | ✅ 10/10 |
| T | Testable（可测试） | 有明确的验收标准，包含≥3 个差异化场景 | ✅ 10/10 |
| D | Designable（可设计） | 包含完整 UI/交互设计约束，无泛化描述 | ✅ 10/10 |

---

## ⚡ 用户确认

**所有 10 个用户故事均通过 INVEST+D 检查，无需重构。**

**请确认下一步操作**：

1. **故事质量**：已全部通过，可直接进入下游流程
2. **下一步建议**：
   - [ ] 继续生成验收标准（Given-When-Then 格式，覆盖全部 10 个故事）
   - [ ] 生成 SRS 规格说明书（整合用户故事 + 业务规则 + 数据模型）
   - [ ] 生成 HTML 交互原型（基于 UI/交互设计约束）

---

> 本文档严格遵循 Deerpowers user-story-generation 技能规范（v1.0）生成，所有故事均通过 INVEST+D 质量检查。
