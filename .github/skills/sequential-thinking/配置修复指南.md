# Sequential Thinking MCP 配置修复指南

## 🔴 错误诊断

你遇到的错误：
```
error: unknown option '--key'
Process exited with code 1
```

**根本原因**：MCP服务器配置使用了错误的参数格式或旧版本配置。

---

## ✅ 正确配置方法

### 方法1：VS Code集成（推荐）

在VS Code中配置MCP，需要在工作区设置中添加：

#### 步骤1：打开设置文件
创建或编辑 `.vscode/settings.json`：

```json
{
  "mcp.servers": {
    "sequential-thinking": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-sequential-thinking"
      ]
    }
  }
}
```

#### 步骤2：验证配置
- 重启VS Code
- 查看输出面板 → MCP → 检查服务器状态

---

### 方法2：Claude Desktop配置

如果你使用Claude Desktop，编辑配置文件：

**Windows路径**：
```
%APPDATA%\Claude\claude_desktop_config.json
```

**macOS路径**：
```
~/Library/Application Support/Claude/claude_desktop_config.json
```

**配置内容**：
```json
{
  "mcpServers": {
    "sequential-thinking": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-sequential-thinking"
      ]
    }
  }
}
```

---

## ❌ 常见错误配置

### 错误1：使用了 `--key` 参数
```json
// ❌ 错误
{
  "command": "npx",
  "args": ["--key", "xxx", "@modelcontextprotocol/server-sequential-thinking"]
}
```

**问题**：`@modelcontextprotocol/server-sequential-thinking` 不需要API密钥。

**修复**：移除 `--key` 参数。

---

### 错误2：使用了smithery CLI参数
```json
// ❌ 错误
{
  "command": "npx",
  "args": ["@smithery/cli", "run", "sequential-thinking", "--key", "xxx"]
}
```

**问题**：这是Smithery市场的配置格式，与直接使用MCP服务器不同。

**修复**：使用方法1或方法2的正确配置。

---

### 错误3：环境变量配置错误
```json
// ❌ 错误
{
  "command": "npx",
  "args": ["-y", "@modelcontextprotocol/server-sequential-thinking"],
  "env": {
    "API_KEY": "xxx"  // 不需要
  }
}
```

**问题**：Sequential Thinking MCP是本地推理工具，不需要外部API。

**修复**：移除 `env` 配置（或仅保留 `DISABLE_THOUGHT_LOGGING` 选项）。

---

## 🔧 完整配置示例

### 最简配置（推荐新手）
```json
{
  "mcp.servers": {
    "sequential-thinking": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sequential-thinking"]
    }
  }
}
```

### 高级配置（禁用日志）
```json
{
  "mcp.servers": {
    "sequential-thinking": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sequential-thinking"],
      "env": {
        "DISABLE_THOUGHT_LOGGING": "true"
      }
    }
  }
}
```

### Docker方式（隔离环境）
```json
{
  "mcp.servers": {
    "sequential-thinking": {
      "command": "docker",
      "args": [
        "run",
        "--rm",
        "-i",
        "mcp/sequentialthinking"
      ]
    }
  }
}
```

---

## 🚀 配置步骤（VS Code）

### 1. 创建配置文件

<function_calls>
<invoke name="run_in_terminal">
<parameter name="command">New-Item -ItemType Directory -Force -Path "c:\Programs\courses-teacher\.vscode" | Out-Null; @'
{
  "mcp.servers": {
    "sequential-thinking": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-sequential-thinking"
      ]
    }
  }
}
'@ | Out-File -FilePath "c:\Programs\courses-teacher\.vscode\settings.json" -Encoding UTF8