# Chrome DevTools + Playwright 测试技能使用指南

> **版本**: v2.0  
> **更新日期**: 2026-01-28  
> **适用范围**: AI辅助E2E测试工作流

---

## 📖 概述

本技能（Skill）提供了一套完整的 **AI辅助E2E测试工作流**，结合 Chrome DevTools MCP 工具与 Playwright 测试框架，实现从页面探索到测试代码生成的全流程自动化。

### 核心特点

- 🔍 **MCP探索**: 使用 Chrome DevTools MCP 自动探索页面结构和交互
- 📝 **先文档后代码**: 先生成测试用例文档，再转化为测试代码
- 🏗️ **POM模式**: 遵循 Page Object Model 设计模式
- 🔄 **闭环诊断**: 测试失败时使用 MCP 快速定位问题

### 5阶段工作流

```
┌─────────────────────────────────────────────────────────────────┐
│  EXPLORE → DOCUMENT → CODE → EXECUTE → DIAGNOSE                 │
│   (MCP)     (AI)      (AI)    (CI)      (MCP)                   │
│     ↑                                      │                    │
│     └──────────── (on failure) ────────────┘                    │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🚀 快速开始

### 前置条件

1. **VS Code** 已安装 GitHub Copilot 扩展
2. **Chrome DevTools MCP** 已配置并可用
3. **Node.js** v18+ 已安装

### 第一步：探索目标页面

使用 MCP 命令探索目标网站：

```javascript
// 1. 打开页面
mcp_io_github_chr_new_page({ url: "https://your-target-site.com" })

// 2. 截图分析
mcp_io_github_chr_take_screenshot({ fullPage: true })

// 3. 获取DOM结构
mcp_io_github_chr_take_snapshot({ verbose: true })

// 4. 提取选择器
mcp_io_github_chr_evaluate_script({
  function: `() => {
    const elements = {};
    document.querySelectorAll('[data-testid], [data-test]').forEach(el => {
      elements[el.dataset.testid || el.dataset.test] = el.tagName;
    });
    return elements;
  }`
})
```

### 第二步：生成测试用例文档

基于探索结果，让 AI 生成测试用例文档：

**提示词示例**：
```
基于以下页面探索结果，生成功能测试用例文档。

系统名称: [你的系统名称]
功能模块: [功能模块路径]

探索结果:
[粘贴MCP探索得到的选择器和交互信息]

请生成：
1. 测试覆盖矩阵
2. 测试用例表格（包含用例编号、优先级、步骤、预期结果）
3. 标记可自动化的用例

输出位置: docs/test-cases/{功能名称}-测试用例.md
```

### 第三步：生成测试代码

基于测试用例文档，生成 Playwright 测试代码：

**提示词示例**：
```
基于以下测试用例文档，生成 Playwright 测试代码。

测试用例文档: [粘贴测试用例文档内容]

要求：
1. 使用 Page Object Model 模式
2. Page Objects 放在 pages/ 目录
3. 测试脚本放在 tests/ 目录
4. 只转化标记为"可自动化"的用例
```

**或使用 AUTO 模式一键生成**：
```
请根据测试用例文档，自动生成所有测试

输入: docs/test-cases/{功能}-测试用例.md
```

### 第四步：执行测试

```bash
# 安装依赖
npm install

# 安装浏览器
npx playwright install chromium

# 运行测试
npx playwright test

# 调试模式
npx playwright test --debug

# UI模式
npx playwright test --ui
```

### 第五步：诊断失败

测试失败时，使用 MCP 命令诊断：

```javascript
// 重现失败场景
mcp_io_github_chr_new_page({ url: "失败的页面URL" })

// 检查控制台错误
mcp_io_github_chr_list_console_messages({ types: ["error"] })

// 检查网络请求
mcp_io_github_chr_list_network_requests({ resourceTypes: ["xhr", "fetch"] })

// 截图当前状态
mcp_io_github_chr_take_screenshot()
```

---

## 📁 目录结构

使用本技能生成的项目推荐以下目录结构：

```
project/
├── docs/                           # 📄 测试文档
│   └── test-cases/                 # 测试用例文档
│       └── {功能名称}-测试用例.md
├── pages/                          # 🏗️ Page Objects
│   ├── LoginPage.ts
│   ├── InventoryPage.ts
│   └── index.ts
├── tests/                          # 🧪 测试脚本
│   ├── fixtures/                   # 测试数据
│   └── *.spec.ts                   # 测试脚本
├── .github/
│   └── workflows/
│       └── e2e.yml                 # CI配置
├── playwright.config.ts
└── package.json
```

---

## 🚀 AUTO模式：一键生成全部测试

当测试用例文档已经准备就绪时，可以使用 AUTO 模式自动完成所有测试代码的生成。

### 触发方式

向 AI 发送以下指令：

```
请根据测试用例文档，自动生成所有测试

输入: docs/test-cases/{功能}-测试用例.md
```

### AI 执行步骤

1. **分析测试文档** - 读取并提取所有「可自动化」用例
2. **生成 Page Objects** - 为每个页面创建 `pages/{PageName}Page.ts`
3. **生成导出文件** - 创建 `pages/index.ts`
4. **生成测试脚本** - 按模块创建 `tests/{module}.spec.ts`
5. **生成 CI 配置** - 创建 `.github/workflows/e2e.yml`
6. **生成配置文件** - 创建 `playwright.config.ts` 和 `package.json`

### 输出示例

```
project/
├── docs/test-cases/购物流程-测试用例.md  # ✅ 输入（已有）
├── pages/                                 # 🆕 生成
│   ├── LoginPage.ts
│   ├── InventoryPage.ts
│   ├── CartPage.ts
│   ├── CheckoutPage.ts
│   └── index.ts
├── tests/                                 # 🆕 生成
│   └── shopping-flow.spec.ts
├── .github/workflows/e2e.yml              # 🆕 生成
├── playwright.config.ts                   # 🆕 生成
└── package.json                           # 🆕 生成
```

### 验证命令

```bash
npm install
npx playwright install chromium
npx playwright test
```

---

## 📋 测试用例文档格式

### 表格字段说明

| 字段 | 说明 | 示例 |
|------|------|------|
| **用例编号** | `{系统}-{模块}-{序号}` | `SWAG-CART-001` |
| **系统** | 被测系统名称 | `Swag Labs` |
| **功能模块** | 模块路径 | `商品管理-购物车` |
| **用例概述** | 简短描述（10-20字） | `添加单个商品到购物车` |
| **优先级** | P0/P1/P2 | `P0` |
| **标签** | 测试类型 | `功能测试,可自动化` |
| **前提条件** | 执行前置条件 | `1. 用户已登录` |
| **输入数据或操作** | 详细步骤 | `1. 点击【Add to cart】` |
| **预期结果** | 量化验证点 | `购物车显示"1"` |
| **测试结果** | 执行结果 | `待测试` |

### 优先级定义

| 优先级 | 说明 | 占比建议 |
|--------|------|----------|
| **P0** | 核心流程，阻塞性功能 | 20-30% |
| **P1** | 重要功能，影响用户体验 | 40-50% |
| **P2** | 辅助功能，优化类需求 | 20-30% |

### 常用标签

- `功能测试` - 正常业务流程
- `异常测试` - 异常场景、错误处理
- `边界测试` - 边界值、临界值
- `参数验证` - 输入参数格式验证
- `可自动化` - 可转化为E2E测试

---

## 🛠️ MCP 常用命令速查

### 页面操作

```javascript
// 打开新页面
mcp_io_github_chr_new_page({ url: "https://example.com" })

// 点击元素
mcp_io_github_chr_click({ uid: "element_uid" })

// 填写表单
mcp_io_github_chr_fill({ uid: "input_uid", value: "text" })

// 批量填写
mcp_io_github_chr_fill_form({
  elements: [
    { uid: "input1", value: "value1" },
    { uid: "input2", value: "value2" }
  ]
})

// 等待文本出现
mcp_io_github_chr_wait_for({ text: "Expected Text", timeout: 5000 })
```

### 信息获取

```javascript
// 截图
mcp_io_github_chr_take_screenshot({ fullPage: true })

// DOM快照
mcp_io_github_chr_take_snapshot({ verbose: true })

// 执行脚本
mcp_io_github_chr_evaluate_script({
  function: `() => document.title`
})

// 控制台消息
mcp_io_github_chr_list_console_messages({ types: ["error", "warn"] })

// 网络请求
mcp_io_github_chr_list_network_requests({ resourceTypes: ["xhr", "fetch"] })
```

---

## 📚 参考资源

### Skill 文件位置

```
.github/skills/chrome-devtools-playwright/
├── SKILL.md                        # 技能入口文件
└── references/
    ├── workflow.md                 # 详细工作流
    ├── prompts.md                  # AI提示词模板
    ├── test-case-template.md       # 测试用例文档模板
    ├── page-object-template.md     # POM模板
    └── diagnosis.md                # 故障诊断模式
```

### 演示项目

完整的 Swag Labs 购物流程演示项目：

```
skill-demo-swag-labs/
├── docs/
│   ├── test-cases/
│   │   └── 购物流程-测试用例.md    # 测试用例文档
│   └── Skill演示执行记录-SwagLabs购物流程.md
├── pages/                          # Page Objects
│   ├── LoginPage.ts
│   ├── InventoryPage.ts
│   ├── CartPage.ts
│   ├── CheckoutPage.ts
│   └── index.ts
├── tests/                          # 测试脚本
│   └── e2e-shopping-flow.spec.ts
├── playwright.config.ts
└── package.json
```

---

## ❓ 常见问题

### Q: 为什么要先生成文档再生成代码？

**A**: 先文档后代码有以下优势：
1. **确保测试覆盖完整性** - 文档阶段可以系统性地梳理测试场景
2. **便于评审和沟通** - 非技术人员也能理解测试范围
3. **支持手动测试** - 在自动化之前可以先手动验证
4. **代码生成更准确** - 有明确的测试用例作为输入，生成的代码更符合预期

### Q: 哪些测试用例适合自动化？

**A**: 标记`可自动化`的用例应满足：
1. UI操作清晰，元素有明确标识
2. 预期结果可通过元素状态验证
3. 不依赖人工判断（如"界面美观"）
4. 测试数据可程序化准备

### Q: MCP探索和直接查看DevTools有什么区别？

**A**: MCP的优势：
1. **可重复** - 命令可以记录和重放
2. **结构化输出** - 选择器直接提取为JSON
3. **AI友好** - 输出格式便于AI分析
4. **自动化** - 可以脚本化探索流程

---

## 📞 获取帮助

- 查看技能文件: `.github/skills/chrome-devtools-playwright/SKILL.md`
- 参考演示项目: `skill-demo-swag-labs/`
- 使用提示词模板: `references/prompts.md`
